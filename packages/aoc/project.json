{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "aoc",
  "projectType": "library",
  "sourceRoot": "packages/aoc/src",
  "tags": ["type_shared", "layer_shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -b packages/aoc/tsconfig.json" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -b packages/aoc/tsconfig.json && tsc -p tests/tsconfig.json" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/aoc tests/aoc" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run tests/aoc",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
